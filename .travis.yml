sudo: required

services:
    - docker

script:
    - make build
    - docker tag `docker images -q |head -1` thefab/autoclean-webdav:latest
    - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
    - docker push thefab/autoclean-webdav
